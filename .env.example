# Copy this file to ".env" and fill in your key.
# Leave the key empty to run in basic mode (no AI, simple commands only).

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# Optional server settings
HOST=127.0.0.1
PORT=5000
